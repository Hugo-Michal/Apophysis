<Window x:Class="FractalFlameCurator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Native Fractal-Flame Curator"
        Width="1460" Height="920" MinWidth="1100" MinHeight="700"
        Background="#0D1218" Foreground="#F3F6FA"
        KeyDown="Window_KeyDown" Closing="Window_Closing">
  <Grid Margin="14">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="330" />
      <ColumnDefinition Width="14" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Border Grid.Column="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" Padding="14">
      <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
          <TextBlock Text="FRACTAL-FLAME CURATOR" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,2" />
          <TextBlock Text="Phase 1 · deterministic generation + manual rating" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,16" />

          <TextBlock Text="Session" FontSize="15" FontWeight="SemiBold" />
          <Label Content="Output directory" />
          <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="38" /></Grid.ColumnDefinitions>
            <TextBox x:Name="OutputDirectoryTextBox" Grid.Column="0" />
            <Button Grid.Column="1" Content="…" Click="BrowseOutput_Click" ToolTip="Choose output directory" />
          </Grid>
          <Label Content="Base random seed" />
          <TextBox x:Name="SeedTextBox" />
          <Grid Margin="0,4,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="10" /><ColumnDefinition /></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0"><Label Content="Batch limit" /><TextBox x:Name="BatchLimitTextBox" /></StackPanel>
            <StackPanel Grid.Column="2"><Label Content="Workers" /><TextBox x:Name="WorkersTextBox" /></StackPanel>
          </Grid>
          <Label Content="Bounded render queue" />
          <TextBox x:Name="QueueCapacityTextBox" />
          <StackPanel Orientation="Horizontal" Margin="-3,7,0,9">
            <Button Content="Start" Click="Start_Click" IsDefault="True" />
            <Button Content="Pause" Click="Pause_Click" />
            <Button Content="Resume" Click="Resume_Click" />
            <Button Content="Stop" Click="Stop_Click" />
          </StackPanel>

          <Separator Margin="0,2,0,10" />
          <TextBlock Text="Render quality" FontSize="15" FontWeight="SemiBold" />
          <TextBlock Text="Built-in CPU renderer · output is always 2048×2048 by default" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,2,0,5" />
          <Label Content="Sample budget (points)" />
          <TextBox x:Name="SampleBudgetTextBox" />
          <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="10" /><ColumnDefinition /></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0"><Label Content="Oversample" /><ComboBox x:Name="OversampleComboBox"><ComboBoxItem Content="1" /><ComboBoxItem Content="2" /><ComboBoxItem Content="3" /></ComboBox></StackPanel>
            <StackPanel Grid.Column="2"><Label Content="Filter radius" /><TextBox x:Name="FilterRadiusTextBox" /></StackPanel>
          </Grid>
          <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="10" /><ColumnDefinition /></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0"><Label Content="Gamma" /><TextBox x:Name="GammaTextBox" /></StackPanel>
            <StackPanel Grid.Column="2"><Label Content="Brightness" /><TextBox x:Name="BrightnessTextBox" /></StackPanel>
          </Grid>
          <Label Content="Vibrancy" />
          <TextBox x:Name="VibrancyTextBox" />
          <Label Content="Palette" />
          <ComboBox x:Name="PaletteComboBox" DisplayMemberPath="Name" />
          <TextBlock Text="Monochrome is the explicit default. Additional palettes are opt-in here." Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,4,0,0" />

          <Separator Margin="0,14,0,10" />
          <TextBlock Text="Status" FontSize="15" FontWeight="SemiBold" />
          <TextBlock x:Name="BackendTextBlock" TextWrapping="Wrap" />
          <TextBlock x:Name="QueueTextBlock" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
          <TextBlock x:Name="SessionTextBlock" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
          <TextBlock x:Name="CurrentTextBlock" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
          <TextBlock x:Name="RatingCountTextBlock" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
          <TextBlock Text="Keyboard: 1–5 rate · S skip · U undo · P pause/resume · Esc stop" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,10,0,0" />
        </StackPanel>
      </ScrollViewer>
    </Border>

    <Border Grid.Column="2" Background="#080B10" BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" Padding="12">
      <Grid>
        <Grid.RowDefinitions><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#020304" BorderBrush="#26313D" BorderThickness="1">
          <ScrollViewer x:Name="PreviewScroll"
                        HorizontalScrollBarVisibility="Auto"
                        VerticalScrollBarVisibility="Auto"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        PreviewMouseWheel="PreviewScroll_MouseWheel"
                        SizeChanged="PreviewScroll_SizeChanged">
            <Grid x:Name="PreviewHost">
              <Image x:Name="PreviewImage" Stretch="Fill" HorizontalAlignment="Center" VerticalAlignment="Center" />
              <TextBlock x:Name="EmptyPreviewTextBlock" Text="Start a session to generate the first flame" Foreground="#718398" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
          </ScrollViewer>
        </Border>
        <Grid Grid.Row="1" Margin="0,12,0,0">
          <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
          <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="1 ★" Tag="1" Click="Rating_Click" FontSize="18" MinWidth="82" />
            <Button Content="2 ★" Tag="2" Click="Rating_Click" FontSize="18" MinWidth="82" />
            <Button Content="3 ★" Tag="3" Click="Rating_Click" FontSize="18" MinWidth="82" />
            <Button Content="4 ★" Tag="4" Click="Rating_Click" FontSize="18" MinWidth="82" />
            <Button Content="5 ★" Tag="5" Click="Rating_Click" FontSize="18" MinWidth="82" />
          </StackPanel>
          <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,3,0,0">
            <Button Content="Skip (S)" Click="Skip_Click" MinWidth="110" />
            <Button Content="Undo (U)" Click="Undo_Click" MinWidth="110" />
            <Button Content="Zoom to fit" Click="ZoomFit_Click" />
            <Button Content="Actual size" Click="ActualSize_Click" />
          </StackPanel>
        </Grid>
      </Grid>
    </Border>
  </Grid>
</Window>
